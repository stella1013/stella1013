name: Latest blog post workflow
on:
  schedule:
    # Runs daily
    - cron: '0 0 * * *'
    
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: Update this repos README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: stella1013/blog-post-workflow@master
